<Window x:Class="Monitor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:Monitor.ViewModels"
        mc:Ignorable="d" d:DataContext="{d:DesignData viewModels:MainViewModel}"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Right" Width="320">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Total work:" HorizontalAlignment="Right" />
            <TextBlock Grid.Column="1" Text="{Binding TotalWork, Mode=OneWay}" HorizontalAlignment="Left" />
            <TextBlock Grid.Column="2" Text="Total idle:" HorizontalAlignment="Right" />
            <TextBlock Grid.Column="3" Text="{Binding TotalIdle, Mode=OneWay}" HorizontalAlignment="Left" />
        </Grid>

        <ListView Grid.Row="1" ItemsSource="{Binding Activities, Mode=OneTime}">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Window" DisplayMemberBinding="{Binding Window}" Width="400" />
                        <GridViewColumn Header="Is Active" DisplayMemberBinding="{Binding IsActive}" Width="120" />
                        <GridViewColumn Header="Duration" DisplayMemberBinding="{Binding Duration}" Width="180" />
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
